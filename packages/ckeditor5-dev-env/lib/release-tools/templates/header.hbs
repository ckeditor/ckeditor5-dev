## {{#unless @root.skipCompareLink~}}
	[{{version}}](
	{{~#if @root.repository~}}
		{{~#if @root.host}}
			{{~@root.host}}/
		{{~/if}}
		{{~#if @root.owner}}
			{{~@root.owner}}/
		{{~/if}}
		{{~@root.repository}}
	{{~else}}
		{{~@root.repoUrl}}
	{{~/if~}}
	{{~#if previousTag~}}
		/compare/{{previousTag}}...{{currentTag}}
	{{~else~}}
		/tree/{{currentTag}}
	{{~/if}})
{{~else}}
	{{~version}}
{{~/unless}}
{{~#if date}} ({{date}})
{{/if}}
