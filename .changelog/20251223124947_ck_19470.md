---
type: Other
scope:
  - ckeditor5-dev-changelog
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19470
---

Automatically normalize the `see` and `closes` fields from changelog entry into arrays when they are literal values.
