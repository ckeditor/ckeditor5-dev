---
type: Major breaking change
scope:
  - ckeditor5-dev-utils
---

Deprecation of theme related utilities due to deprecation of the `ckeditor5-theme-lark` package.

The `themeImporter()` function is no longer exported. The `getDllPluginWebpackConfig()` function no longer accepts the `themePath` argument.
