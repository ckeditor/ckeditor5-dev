---
type: Fix
scope:
  - ckeditor5-dev-ci
closes:
  - ckeditor/ckeditor5#20024
---

Fixed `ckeditor5-dev-ci-trigger-snyk-scan` so it can locate the `snyk` binary in `pnpm` installations using the `node-linker=hoisted` setting.

Previously, the command could fail with a "not found" error in hoisted setups.
