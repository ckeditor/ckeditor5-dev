---
type: Feature
scope:
  - ckeditor5-dev-release-tools
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19874
---

Added support for publishing stable releases with the `latest-v{X}` dist-tag, including strict validation that the tag major matches the released version major.

Existing tag behavior (`latest`, `staging`, `next`, pre-release tags, and custom tags) remains backward compatible.
