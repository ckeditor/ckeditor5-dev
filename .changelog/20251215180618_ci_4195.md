---
type: Major breaking change
scope:
  - ckeditor5-dev-build-tools
---

The `build()` function will no longer add banner to `**/*.d.ts` files. It will now instead add it to stricter `**/translations/**/*.d.ts` files. Other included files (`**/*.js`, `**/*.css`) are unchanged.
