---
type: Major breaking change
scope:
  - ckeditor5-dev-utils
---

Removal of theme related utilities due to deprecation of the `ckeditor5-theme-lark` package.

The `themeImporter()` function was removed. The `getDllPluginWebpackConfig()` function no longer accepts the `themePath` argument.
