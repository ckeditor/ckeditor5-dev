---
type: Feature
scope:
  - ckeditor5-dev-changelog
---

Added a generated `--help` screen to `ckeditor5-dev-changelog-create-entry` and switched its option parsing to `cac` so the CLI help stays in sync with the supported options.
