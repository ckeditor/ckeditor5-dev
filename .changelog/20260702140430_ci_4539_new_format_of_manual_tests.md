---
type: Feature
scope: ckeditor5-dev-manual-server
---

Added the `rawSvgPlugin()` Vite plugin that loads `.svg` files as raw source strings, and the `renderMarkdown()` helper that converts Markdown to HTML using the same pipeline the manual test server used for instruction files.
