---
type: Major breaking change
scope:
  - ckeditor5-dev-release-tools
---

The deprecated `findPathsToPackages()` and `checkVersionAvailability()` functions are no longer exported. Use the `workspaces` and `npm` namespaces from `@ckeditor/ckeditor5-dev-utils` instead.
