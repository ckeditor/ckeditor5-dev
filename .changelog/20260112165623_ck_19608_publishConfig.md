---
type: Feature
scope:
  - ckeditor5-dev-release-tools
closes:
  - https://github.com/ckeditor/ckeditor5/issues/19608
---

The `prepareRepository()` task now applies `publishConfig` overrides directly to copied `package.json` files.

Fields defined in `publishConfig` replace their top-level counterparts in the final manifest, and the `publishConfig` field itself is removed from the published file.
