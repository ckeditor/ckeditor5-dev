---
type: Feature
scope: ckeditor5-dev-manual-server
---

Added the `manualStaticAssetsPlugin()` Vite plugin that serves and emits static assets used by manual tests. This logic was previously part of the `manualTestsPlugin()` plugin, which no longer handles static assets on its own.
