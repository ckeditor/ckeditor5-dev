---
type: Other
scope:
  - ckeditor5-dev-tests
---

The testing environment no longer consumes the `themePath` option in both test runner tasks.

Previously `themePath` was hardcoded to `@ckeditor/ckeditor5-theme-lark`.
